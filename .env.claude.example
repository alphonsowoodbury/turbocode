# Claude Code Integration Configuration

# Enable/disable automatic Claude responses to user comments
# Set to "false" to disable the webhook
CLAUDE_AUTO_RESPOND=true

# Turbo API URL (used by MCP server and webhook service)
TURBO_API_URL=http://localhost:8001/api/v1

# Optional: Custom path to Claude Code CLI
# Defaults to "claude" in PATH
# CLAUDE_CLI_PATH=/usr/local/bin/claude